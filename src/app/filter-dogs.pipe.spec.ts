import { FilterDogsPipe } from './filter-dogs.pipe';

describe('FilterDogsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDogsPipe();
    expect(pipe).toBeTruthy();
  });
});
